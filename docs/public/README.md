# public

Static files for the docs site. Anything here is served as is at the site root, so
`pred-logo.svg` is reachable at `/pred-logo.svg`.

Right now this holds the Pred logos used in the docs header.
