# public

Static files served as is at the site root. For example, `btc.svg` is reachable at
`/btc.svg`.

This holds the logos and the token and asset icons used around the app (the Pred logo, BTC,
SUI, USDC).
