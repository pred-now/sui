# hooks

Small reusable React hooks.

- `use-now.ts` Returns the current time and ticks on an interval. Used for live countdowns,
               like the time left on a market and the unstake cooldown, so components
               re-render as the clock moves.
