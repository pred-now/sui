# leaderboard

The leaderboard page. It shows the top traders ranked by return, with their earnings and
return on investment. It fetches the ranking from the backend leaderboard endpoint and
renders it as a simple table.
